import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import ConfirmarDomicilio from './components/ConfirmarDomicilio';
ReactDOM.render(
  < ConfirmarDomicilio />,
  document.getElementById('root')
);
